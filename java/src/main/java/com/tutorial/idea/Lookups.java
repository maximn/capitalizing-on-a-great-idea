package com.tutorial.idea;

public class Lookups {
    // By class name (App) (CMD + O) [same shortcut again for out of the project lookup]
    // By symbol (nextDouble) (CMD + ALT + O)
    // By filename (.gradle) (CMD + SHIFT + O) - `/` will take to folders

    // CamelCase - UnsOpEx
    // Wildcard matching - Da*me
    // namespaces - joda.DT
}
